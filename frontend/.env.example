# REST API
# Local dev: leave empty or http://localhost:5000/api → CRA proxies /api → :5000
# Production: prefer https://lyceum.vercel.app/api (same-origin; vercel.json rewrites to lyceum-server)
# If REACT_APP_API_URL still points at lyceum-server.vercel.app, the app auto-uses window.location.origin/api
REACT_APP_API_URL=

# Socket.IO server (LyceumSocket)
REACT_APP_SOCKET_URL=http://localhost:5001

# PeerJS signaling server (LyceumPeerServer)
REACT_APP_PEER_HOST=localhost
REACT_APP_PEER_PORT=9000

# Google OAuth
REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# Google Cloud Console → Credentials → OAuth client:
#   Authorized JavaScript origins:
#     - http://localhost:3000
#     - https://lyceum.vercel.app
#   (Popup sign-in uses POST /api/auth/google — no redirect URI required for GIS button flow)
